% thiago vicente - 121497
%% load data
clear
clc
load("saved/data.mat")

%%
f = uifigure('Name', 'Search Similar Reviews');
searchField = uieditfield(f, 'text', 'Position', [80, 350, 300, 30]);

%% find similar
threshold = 0.1;
similar = findSimilar('very good app',...
                    shingle_size,MH, ...
                    threshold,R);


disp(reviews(indices(similar)));